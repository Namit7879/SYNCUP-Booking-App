{
  "name": "booking-app",
  "version": "1.0.0",
  "scripts": {
    "start": "cd server && npm run prisma:migrate && npm start",
    "postinstall": "cd server && npm install && npm run prisma:generate"
  }
}
